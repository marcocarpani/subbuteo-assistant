services:
  - type: web
    name: subbuteo-assistant-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # Inserita manualmente nella dashboard Render
