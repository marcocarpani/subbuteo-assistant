[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — tutte le route vanno a index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
